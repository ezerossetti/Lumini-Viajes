Componente React + Tailwind renderizado a HTML estático para la landing.

Compilar desde esta carpeta: `node build.mjs`. Usa las dependencias ya instaladas en `../coverage/node_modules`; para restaurarlas ejecutar `npm ci` en coverage (sin ejecutar su build).

Editar QuoteSection.tsx y editorial.css; el build actualiza únicamente Cotización y quote.css. La lógica de WhatsApp existente en script.js sigue enlazada mediante #trip-form.
