Navbar React compilada a HTML estático para la landing.

Editar Navbar.jsx y ejecutar `node build.mjs` desde esta carpeta. Usa las dependencias ya instaladas en ../coverage/node_modules. Los estilos están en ../../navbar.css y el indicador activo en ../../navbar.js. No se agrega React al navegador.
